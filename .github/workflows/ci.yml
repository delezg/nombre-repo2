name: CI

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:

    - name: Ver la hora
      run: date

    - name: Esperar 3 segundos
      run: sleep 3

    - name: Esperar 3 segundos
      run: git version